# malicious_url_detection
Machine learning-based malicious URL detector using Logistic Regression and TF-IDF vectorization. Analyzes URL patterns through custom tokenization (splitting by slashes, hyphens, and dots) to classify URLs as safe or malicious with ~96% accuracy. Includes IP geolocation lookup for threat intelligence. Built with scikit-learn and pandas.
